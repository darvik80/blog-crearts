spring.application.name=core-service
spring.config.name=${spring.application.name}
spring.profiles.active=dev

server.port=7003

# management
management.server.port=7004
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# discovery
eureka.client.enabled=false
eureka.client.service-url.defaultZone=http://localhost:7001/eureka/

# config
spring.cloud.refresh.enabled=true
spring.cloud.config.uri=http://localhost:7001/config

# Db
spring.r2dbc.url=r2dbc:mysql://localhost:3306
spring.r2dbc.name=crearts
spring.r2dbc.username=darvik
spring.r2dbc.password=1234